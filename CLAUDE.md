# CLAUDE.md — finder-project 상주 규칙

이 파일은 모든 Claude Code 세션이 시작 시 읽는 규칙이다. 아래 내용은
세션 중 어떤 요청보다 우선한다.

## 역할과 구현 권한

2026-08-28부터 기존의 펌웨어 구현 금지 규칙을 폐기한다. 대회 제출 일정과
실제 협업 상황에 맞춰 다음 역할을 적용한다.

- **이휘(개발 책임자)**: 펌웨어·규격·시뮬레이터·웹 데모·문서의 구현과
  통합을 담당한다.
- **김민수(팀원)**: 구현 내용을 직접 실행·검증하고, 실측·시연·발표 준비와
  심사 질문에 답할 수 있도록 동작 원리를 숙지한다.
- **AI 코딩 보조**: 이휘의 지시에 따라 저장소 전 영역의 코드 작성·수정,
  테스트, 문서화를 지원할 수 있다. `firmware/node/`와
  `firmware/handset/`도 포함한다.
- 제출 문서에는 사람과 AI의 기여를 사실대로 구분한다. 아직 수행하지 않은
  작업을 특정 인물이 직접 구현했다고 기록하지 않는다.
- 모든 문서·주석·응답은 한국어로 쓴다. OLED의 기본 글꼴처럼 한글 표시가
  불가능한 장치 화면은 짧은 영문을 사용할 수 있다.

## 기술 스택 요약

- 보드: LILYGO T3 LoRa32 **V1.6.1** × 4 (ESP32-PICO-D4 + SX1278 +
  0.96" SSD1306 OLED, 433/470 MHz 판). 시설 노드 3대 + 수신 단말 1대.
- 시험 무선 설정: **433.92 MHz**, 125 kHz, SF7, 4/5, 출력 2 dBm부터
  시작한다. 안테나를 연결하지 않은 상태에서는 송신하지 않는다.
- 실제 송신 전 해당 제품의 KC 적합성평가 여부와 국내 기술기준 충족 여부를
  별도로 확인한다. 소프트웨어 설정값만으로 적합성을 단정하지 않는다.
- 라이브러리: LoRa는 **RadioLib**, BLE는 ESP32 기본 스택.
- 공유 규격: `firmware/common/packet.h` — 노드·단말·시뮬레이터가 공유.
  규격을 바꾸면 `sim/mesh_sim.py`의 상수도 함께 맞춘다.
- 시뮬레이터: 파이썬 3 표준 라이브러리만 사용(`sim/`).
- 웹 데모: 단일 HTML + 카카오맵 JS API(`webdemo/`).
- 결정적 시연: 발신 A → 중계 B/C → 수신 D 구성에서 B 전원을 뽑아도
  A→C→D로 우회해 수신이 유지됨을 보인다.

## 커밋 규약

- 논리 단위 1커밋 (컴파일/실행 가능한 상태 + 한 가지 변화).
- 메시지는 `영역: 한 줄 요약` 형식.
  - 예: `sim: 노드 소실 우회 테스트 추가`
  - 영역 예시: `repo`, `common`, `sim`, `webdemo`, `docs`, `node`,
    `handset`, `data`

## 리뷰 모드 지침

사용자가 명시적으로 "리뷰만" 요청한 경우에는 코드를 직접 수정하지 않고
다음 형식의 목록으로 출력한다:

```
파일:줄번호 — 코멘트
```

- 코멘트는 고등학생 멘티에게 맞는 톤(격려 + 질문형)으로 쓴다.
- 수정 요청이 포함된 경우에는 구현과 검증까지 진행할 수 있다.
